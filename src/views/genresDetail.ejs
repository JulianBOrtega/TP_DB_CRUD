<!DOCTYPE html>
<html lang="es">
    <%- include('./partials/head.ejs') %> 
    <body>
        <%- include('./partials/header.ejs') %> 
        
        <main>
            <h1><%= genre.name %> </h1>
            <br>
            <p>NAME: <%= genre.name %> </p>
            <p>RANKING: <%= genre.ranking %> </p>
        
            <br>
            <h4>Películas</h4>
            <ul>
                <% genre.movies.forEach(movie => { %>
                <li><a href="/movies/detail/<%= movie.id %>"><%= movie.title %></a></li>
                <% }) %>
            </ul>
        </main>
    </body>
</html>